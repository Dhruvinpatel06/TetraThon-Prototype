services:
  - type: web
    name: agritech-backend
    env: python
    buildCommand: pip install -r Backend/requirements.txt
    rootDir: Backend
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.1
